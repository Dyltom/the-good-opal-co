{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "pnpm payload generate:importmap && pnpm payload migrate && pnpm import:woocommerce && pnpm build",
  "regions": ["syd1"],
  "functions": {
    "src/app/api/webhooks/stripe/route.ts": {
      "maxDuration": 30
    }
  }
}
