# Claude Code Compatibility

This repository is now Codex-first.

Use `../AGENTS.md` as the source of truth for project instructions. The
Claude-specific command files under `.claude/commands/` are legacy references
from the previous setup. New reusable workflows should be added as Codex skills
under `.agents/skills/`.
